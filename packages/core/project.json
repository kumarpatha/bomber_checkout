{
  "name": "core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/core/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack --mode production"
      },
      "dependsOn": [
        {
          "target": "generate"
        },
        {
          "target": "build",
          "dependencies": true
        }
      ]
    },
    "prerelease-build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\"",
          "node -e \"const { spawn } = require('child_process'); const p = spawn('npx', ['webpack', '--mode', 'production'], { stdio: 'inherit', shell: true, env: { ...process.env, PRERELEASE: 'prerelease' } }); p.on('exit', (code) => process.exit(code ?? 0));\""
        ],
        "parallel": false
      },
      "dependsOn": [
        {
          "target": "generate"
        },
        {
          "target": "build",
          "dependencies": true
        }
      ]
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"const { spawn } = require('child_process'); const p = spawn('npx', ['webpack', '--mode', 'development', '--watch'], { stdio: 'inherit', shell: true, env: { ...process.env, MEASURE_SPEED: 'false' } }); p.on('exit', (code) => process.exit(code ?? 0));\""
      },
      "dependsOn": [
        {
          "target": "generate"
        },
        {
          "target": "build",
          "dependencies": true
        }
      ]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/packages/core"],
      "options": {
        "jestConfig": "packages/core/jest.config.js",
        "passWithNoTests": false,
        "runInBand": true
      },
      "dependsOn": [
        {
          "target": "generate"
        }
      ]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "fix": true,
      "dependsOn": [
        {
          "target": "generate"
        }
      ]
    },
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "node -e \"require('fs').rmSync('packages/core/src/app/generated', { recursive: true, force: true })\"",
          "npx nx generate @bigcommerce/checkout/workspace-tools:auto-export --projectName=core",
          "npx nx generate @bigcommerce/checkout/workspace-tools:polyfill-generator --projectName=core"
        ],
        "parallel": false
      }
    }
  },
  "tags": ["scope:core"]
}
